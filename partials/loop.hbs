<article class="gh-card {{post_class}}">
    <a class="gh-card-link" href="{{url}}">
        <div class="gh-card-aside">
            <span class="gh-card-hash" title="{{id}}">{{id}}</span>
            <time class="gh-card-date" datetime="{{date format="YYYY-MM-DD"}}">{{date format="YYYY-MM-DD"}}</time>
        </div>
        <div class="gh-card-body">
            <header class="gh-card-header">
                <h2 class="gh-card-title">{{title}}</h2>
            </header>
            <div class="gh-card-excerpt">{{excerpt}}</div>
        </div>
        <footer class="gh-card-meta">
            <span class="gh-card-duration">{{reading_time minute=(t "1 min read") minutes=(t "% min read")}}</span>
            {{#if @site.comments_enabled}}
                {{comment_count class="gh-card-comments"}}
            {{/if}}
            {{^has visibility="public"}}
                {{> icons/star}}
            {{/has}}
        </footer>
    </a>
</article>
